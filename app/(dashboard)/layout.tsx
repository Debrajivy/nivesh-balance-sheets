import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{PwaStatus}from"@/components/pwa-status";import{LogoutButton}from"@/components/logout-button";import{DatabaseRecords}from"@/components/database-records";import{getSession}from"@/lib/server/auth";export default async function DashboardLayout({children}:{children:React.ReactNode}){let session=null;try{session=await getSession()}catch{}if(!session)redirect("/login");return <><PwaStatus/><LogoutButton/><DatabaseRecords/><AppShell>{children}</AppShell></>}
